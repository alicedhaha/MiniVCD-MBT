# MiniVCD-MBT 项目申报书

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | MiniVCD-MBT |
| 参赛者 | 黄东红 |
| 邮箱 | alicedhaha@qq.com |
| 手机号 | 19287585536 |
| GitHub 仓库链接 | https://github.com/alicedhaha/MiniVCD-MBT |
| Gitlink 仓库链接 | https://gitlink.org.cn/alicedhh/MiniVCD-MBT |
| Mooncakes 包 | https://mooncakes.io/docs/alicedhaha/minivcd_mbt |
| 项目方向 | MoonBit 工程工具 / 数字电路波形解析与查询 |
| 项目类型 | 原创项目 |
| 账号归属说明 | GitHub 账号 alicedhaha 与 GitLink 账号 alicedhh 均为参赛者黄东红本人在不同平台使用的昵称 |

## 项目简介

MiniVCD-MBT 是一个使用 MoonBit 编写的轻量级 VCD 波形解析和命令行查看工具。项目聚焦于数字电路开发中最常见的基础需求：读取合法 VCD 文件、解析层级信号与时间戳变化、查询某个信号在指定时刻的值，并将变化记录导出为统一 CSV。项目不追求复杂 EDA 能力，而是强调小而完整、可运行、可测试、可发布，适合作为 MoonBit 在工程工具方向上的一个可复用示例。

## 项目方向与适用场景

本项目面向 MoonBit 工程工具与应用生态建设，针对数字电路开发、教学实验、RTL 调试和波形数据预处理场景提供基础能力。适用场景包括：在没有重型 GUI 工具时快速检查 VCD 文件内容、在教学中演示数字信号变化过程、在脚本化流程中提取信号变化记录、以及作为后续更复杂波形工具的基础解析模块。

## 核心功能

1. 解析 VCD 头部信息，包括 `$date`、`$version`、`$timescale`、`$scope`、`$upscope`、`$var` 和 `$enddefinitions`。
2. 支持层级作用域，能够为信号生成完整名称，例如 `top.cpu.clk`。
3. 支持四态标量值 `0`、`1`、`x`、`z`，并兼容大小写 `X`、`Z`。
4. 支持二进制向量值解析与校验，例如 `b0001`、`b1010`、`bxxxx`。
5. 支持时间戳与信号变化记录解析，可查询指定信号的全部变化历史和指定时刻的最近值。
6. 提供基础统计能力，包括 change count、first change time、last change time、rising edges 和 falling edges。
7. 提供文件级摘要能力，以及按 identifier 查询信号和变化记录的辅助 API，便于二次开发接入。
8. 提供信号描述、作用域/类型筛选、时间戳分桶、快照、覆盖率摘要、解析后校验诊断、Markdown/文本报告和 VCD 回归比较能力。
9. 提供统一 CSV 导出格式，支持全量导出和单信号导出，便于后续分析或接入其他工具。
10. 提供命令行工具 `minivcd`，支持 `info`、`signals`、`changes`、`stats`、`value`、`export` 等查询与导出命令。

## 项目性质与开源合规

本项目为原创实现，核心逻辑由 MoonBit 编写完成，未直接移植现有 VCD 解析器代码。开发前已在 mooncakes.io 通过 `vcd`、`value change dump`、`verilog waveform`、`waveform parser` 等关键词做过撞题检查，未发现功能高度重复的成熟 MoonBit 项目。项目采用 MIT License，仓库根目录已提供 `LICENSE` 文件；示例文件、测试代码和文档均随仓库公开发布，便于复现、检查和继续维护。参赛材料中使用的 GitHub 账号 `alicedhaha` 与 GitLink 账号 `alicedhh` 均为参赛者黄东红本人账号，仅为平台昵称不同，不涉及多人协作或代提交。

## 质量与边界

项目当前已经完成可运行闭环：`moon check`、`moon test`、CLI 示例命令和 Mooncakes 发布均已通过验证。仓库包含 README、测试、示例 VCD、CI 和发布配置，适合作为一个小型但完整的 MoonBit 工程项目。当前 MoonBit 源码物理行数 5378 行、非空行 4921 行；按更严格的“非空且非注释”口径统计，有效 MoonBit 代码 4320 行，其中产品有效代码 2518 行、测试有效代码 1802 行。测试用例 240 个，覆盖解析、错误处理、查询、统计、CSV、诊断、快照、覆盖率、报告和 VCD 回归比较功能。项目边界也保持清晰：当前仅支持 VCD 文本解析、信号查询、基础统计、解析后诊断、文本/Markdown 报告、VCD 对比和 CSV 导出，不包含 FSDB/FST、GUI、Verilog 编译仿真、波形绘图或高级时序分析等重型功能，重点是把基础能力做实、做准、做成可复用工具。
